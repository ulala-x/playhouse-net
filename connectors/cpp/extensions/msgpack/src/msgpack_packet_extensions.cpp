// MessagePack extension is header-only, this file is intentionally empty
// It exists to satisfy build system expectations for a source file
#include <playhouse/extensions/msgpack/msgpack_packet_extensions.hpp>

// Explicit template instantiation for common types (optional)
namespace playhouse::extensions::msgpack {
    // Implementation is in the header file
}

// Protobuf extension is header-only, this file is intentionally empty
// It exists to satisfy build system expectations for a source file
#include <playhouse/extensions/proto/proto_packet_extensions.hpp>

// Explicit template instantiation for common types (optional)
namespace playhouse::extensions::proto {
    // Implementation is in the header file
}
